# hehe not here :p
from utils.context import ExtensionContext


def extend_core(context: ExtensionContext):
    _core = context.core

    class Varchar():
        
        def generate(self):
            return 

